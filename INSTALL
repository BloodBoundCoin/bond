Building BLOOD_BOUND_MASTERNODE

Use the autogen script to prepare the build environment.

    ./autogen.sh
    ./configure
    make

Always verify the signatures and checksums.

See doc/build-*.md for instructions on building BLOOD_BOUND_MASTERNODEd,
the intended-for-services, no-graphical-interface, reference
implementation of BLOOD_BOUND_MASTERNODE.
